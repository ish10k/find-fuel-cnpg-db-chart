apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: {{ .Values.operatorArgoApplication.name }}
  namespace: {{ .Values.operatorArgoApplication.namespace }}
spec:
  project: default
  source:
    repoURL: https://github.com/cloudnative-pg/charts
    targetRevision: main
    path: charts/cloudnative-pg
  destination:
    server: https://kubernetes.default.svc
    namespace: {{ .Values.namespace }}
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
  syncOptions:
    - ServerSideApply=true